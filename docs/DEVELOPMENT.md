# Development Guide

## Folder Structure

apps/cli → Main CLI tool
apps/web → Dashboard
packages/bridge → Backend
packages/ao-processes → AO agents
mcp/simple-mcp → X tools

## Adding New Features
Add command in apps/cli/src/index.ts
Implement logic in commands/
Update Bridge if needed

